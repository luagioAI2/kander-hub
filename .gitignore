# Locally generated directories, not repository content
.code-review-graph/
.memsearch/

# Build and system artifacts
/kander
*.exe
*.test
*.out
.DS_Store
Thumbs.db

# kanban/ is not listed here: `kander init` adds it to .git/info/exclude,
# and rules/KANDER-KANBAN-RULES.md forbids propagating the local board via the project .gitignore.

/worktrees/
/tmp/

